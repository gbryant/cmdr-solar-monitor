#pragma once

// Copy this file to secrets.h and fill in your network. secrets.h is gitignored
// so credentials never land in the repo.
#define WIFI_SSID     "your-network"
#define WIFI_PASSWORD "your-password"
